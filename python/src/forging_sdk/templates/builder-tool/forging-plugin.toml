[plugin]
name = "{{ name }}"
version = "0.1.0"
description = "{{ description }}"
author = "{{ author }}"
license = "MIT"
min_forging_version = "0.2.0"

[plugin.builder_tools]
module = "{{ module_name }}"
register_function = "register"
